import {dummyFortWayneJSON} from "@/API-Calls/dummyFunctionsAndData/dummyFortWayneJSON";

export default async function pingAllWeatherAPIs(city){
	if (city) {
		const response = dummyFortWayneJSON

		return response
	}
}